<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Welcome to Workout Tracker</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/styles.css">
  <style>
    body {
      margin: 0;
      font-family: 'Arial', sans-serif;
      height: 100vh;
      background: linear-gradient(270deg, #121212, #1e1e1e, #343434);
      background-size: 600% 600%;
      animation: GradientAnimation 10s ease infinite;
    }
    @keyframes GradientAnimation {
      0% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
      100% { background-position: 0% 50%; }
    }
    .content-wrapper {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      text-align: center;
      color: #f8f9fa;
    }

    h1 {
      font-size: 4rem;
      font-weight: bold;
      margin-bottom: 20px;
    }

    p {
      font-size: 1.5rem;
      margin-bottom: 30px;
      color: #dcdcdc;
    }

    .btn-custom {
      font-size: 1.25rem;
      padding: 10px 20px;
      border: 2px solid #ccc;
      color: #f8f9fa;
      background: transparent;
      transition: all 0.3s ease-in-out;
    }

    .btn-custom:hover {
      background: #f8f9fa;
      color: #121212;
      border-color: #f8f9fa;
    }
  </style>
</head>
<body>
  <div class="content-wrapper">
    <h1>Welcome to Workout Tracker</h1>
    <p>Track your workouts and achieve your fitness goals with ease.</p>
    <a href="/workouts" class="btn btn-custom">Enter Workout</a>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
